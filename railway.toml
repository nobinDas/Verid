[build]
builder = "nixpacks"
buildCommand = "cd finance-backend && ./mvnw clean package -DskipTests"

[deploy]
startCommand = "java -jar finance-backend/target/*.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
